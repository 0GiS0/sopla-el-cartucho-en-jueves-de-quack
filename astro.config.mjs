import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://0GiS0.github.io',
  base: '/sopla-el-cartucho-en-jueves-de-quack',
  output: 'static',
  server: { host: true },
});
